function setup() {
  createCanvas(400, 400);
  background(30);
  fill(255);
  textAlign(CENTER, CENTER);
  textSize(32);
  text('Hello, world!', width / 2, height / 2);
}

function draw() {
  // No animation for now
}
